# 🚶 Thinking Box Walkthrough

이 문서는 Thinking Box 프로젝트의 구조, 동작 방식, 그리고 사용법을 설명합니다.

---

## 📌 프로젝트 개요

**Thinking Box**는 회의 중 발생하는 의사결정을 실시간으로 추적하고 분석하는 AI 회의 도우미입니다.

### 핵심 컨셉: "Turnback" 감지
"Oh My Mistake" 논문에서 영감을 받아, 회의 중 "말 바꾸기"를 자동으로 감지합니다:
- **"아, 3시 말고 4시로 하자"** → 시간 결정 변경 감지
- **"역시 처음 아이디어가 나았어"** → 이전 의견으로 복귀 감지

---

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    🎤 사용자 음성 입력                       │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              [Frontend] Web Speech API                       │
│              (브라우저 내장 음성 인식)                        │
└─────────────────────────────────────────────────────────────┘
                            │ WebSocket (텍스트)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              [Backend] FastAPI Server                        │
│              - /ws/text/{meeting_id}                         │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              [AI Agent] Meaning Filter                       │
│              - LangChain + Gemini 2.5                        │
│              - 결정사항 추출                                  │
│              - Turnback 감지                                 │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              [Frontend] 3-Panel UI                           │
│              - 실시간 자막                                    │
│              - 즉시 감지 알림                                 │
│              - 누적 회의 요약                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 주요 컴포넌트 설명

### 1. useAudioStream Hook (Frontend)
`thinking_box_frontend/hooks/useAudioStream.ts`

**역할:**
- Web Speech API 초기화 및 관리
- WebSocket 연결 관리
- 화자 상태 관리
- 자막 및 분석 결과 상태 관리

**주요 함수:**
- `startRecording()` - 음성 인식 시작
- `stopRecording()` - 음성 인식 중지
- `switchSpeaker(id)` - 화자 전환 (수동 모드)
- `addSpeaker()` - 새 화자 추가

### 2. Meaning Filter Agent (Backend)
`thinking_box_backend/app/services/meaning_filter.py`

**역할:**
- 텍스트 분석 및 구조화된 정보 추출
- 결정사항(Decisions) 식별
- Turnback 이벤트 감지

**입력:**
```json
{
  "text": "아 3시 말고 4시로 하자",
  "context_topics": ["회의 시간"]
}
```

**출력:**
```json
{
  "main_topics": ["회의 시간"],
  "decisions": [{"topic": "회의 시간", "value": "4시", "status": "변경"}],
  "turnback": {
    "type": "SINGLE_CHANGE",
    "target_topic": "회의 시간",
    "new_value": "4시",
    "reason": "3시에서 4시로 변경"
  },
  "is_chitchat": false
}
```

### 3. Stream Endpoint (Backend)
`thinking_box_backend/app/api/endpoints/stream.py`

**역할:**
- WebSocket 연결 관리
- 텍스트 수신 및 Meaning Filter 호출
- 분석 결과 클라이언트로 전송

---

## 🖥️ UI 구성

### 3-Panel 레이아웃

```
┌─────────────┬─────────────┬─────────────┐
│  실시간     │   즉시      │   회의      │
│  자막       │   감지      │   진행      │
│             │             │   요약      │
└─────────────┴─────────────┴─────────────┘
```

**1. 실시간 자막 패널**
- 모든 발화 내용 표시
- 화자별 색상 태그
- 중간 결과는 🎤 아이콘으로 표시

**2. 즉시 감지 패널**
- 최근 5개 분석 결과만 표시
- 결정사항: 초록색 카드
- Turnback: 빨간색 경고

**3. 회의 진행 요약 패널**
- 주제(안건)별 그룹핑
- 각 주제 내 결정사항 목록
- Turnback은 취소선 + 새 값 표시

---

## 🧪 테스트 방법

### 기본 테스트
1. 백엔드 서버 실행: `python main.py`
2. 프론트엔드 실행: `npm run dev`
3. http://localhost:3000 접속
4. "회의 시작" 클릭
5. 마이크에 대고 말하기
6. 3개 패널 모두 업데이트 확인

### Turnback 테스트
다음과 같이 말해보세요:
> "회의 시간은 3시로 하겠습니다... 아 잠깐, 3시 말고 4시로 변경할게요"

→ 빨간색 Turnback 알림이 표시되어야 함

---

## 🔧 설정 변경

### 화자 구분 모드
- 우측 상단 ⚙️ 설정 클릭
- 수동 / AI 자동 / 끄기 선택

### 시스템 오디오 캡처 (VB-Audio)
1. VB-Audio Virtual Cable 설치
2. 시스템 출력을 "CABLE Input"으로 설정
3. Chrome 마이크를 "CABLE Output"으로 선택

---

## 📝 개발 노트

### 왜 Web Speech API를 사용하나요?
- 브라우저 내장 → 별도 서버 리소스 불필요
- 한국어 인식 정확도 우수
- 무료 (Google Cloud STT는 유료)

### 왜 서버에서 STT를 하지 않나요?
- 초기에 Gemini로 STT 시도 → 환각(Hallucination) 문제 발생
- Web Speech API가 훨씬 안정적

### 향후 개선 계획
- AI 기반 화자 감지 (음성 특징 분석)
- Supabase 연동 (회의 기록 저장)
- 다중 사용자 지원

---

## 🐛 알려진 이슈

1. **AI 화자 감지**: 현재 UI만 구현됨
2. **긴 회의**: 메모리 사용량 증가 가능
3. **Firefox**: Web Speech API 미지원

---

*마지막 업데이트: 2026-01-08*
